# `path` module
`path` module is a built-in nodeJS module that provide tools for handling and transforming file paths across different operating systems. 

- Windows have backslashes(\) and linux have forward slashes(/).
- `path` module helps write cross-platform code that works on both OS.

